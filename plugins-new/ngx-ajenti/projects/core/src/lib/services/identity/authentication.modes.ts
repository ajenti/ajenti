/**
 * Defines the possible authentication modess.
 */
export enum AuthenticationModes {

  /**
   * The Normal authentication mode authenticates the user with normal rights.
   */
  Normal = 'normal',

  /**
   * The Sudo authentication mode authenticates the user with sudo rights.
   */
  Sudo = 'sudo',

}
