import { Component, ViewEncapsulation } from '@angular/core';

@Component({
  encapsulation: ViewEncapsulation.None,
  selector: 'shared-styles',
  template: '',
  styleUrls: [ './shared-styles.component.less' ],
})
export class SharedStylesComponent {
}
