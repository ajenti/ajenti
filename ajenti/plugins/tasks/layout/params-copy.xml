<bind:dict id="bind">
    <vc>
        <formline text="{Source directory}">
            <pathbox directory="True" bind="source" />
        </formline>
        <formline text="{Destination directory}">
            <pathbox directory="True" bind="destination" />
        </formline>
    </vc>
</bind:dict>